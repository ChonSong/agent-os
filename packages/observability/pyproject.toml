[project]
name = "@agent-os/observability"
version = "0.0.0"
description = "AIE-compatible observability layer"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["observability"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
